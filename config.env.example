# LED Panel Display Configuration
# Copy this file to config.env and fill in your values

# === Hardware Settings ===
# Panel dimensions
PANEL_WIDTH=64
PANEL_HEIGHT=20
PANEL_COUNT=2

# === BLE Panel Settings ===
# Your LED panel BLE addresses (find using BLE scanner app)
BLE_ADDRESS_TOP=YOUR-TOP-PANEL-BLE-ADDRESS-HERE
BLE_ADDRESS_BOTTOM=YOUR-BOTTOM-PANEL-BLE-ADDRESS-HERE

# BLE characteristic UUID for your panels
# Default works for most 64x20 LED panels
BLE_UUID_WRITE=0000fa02-0000-1000-8000-00805f9b34fb

# === Weather Settings ===
# Get free API key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=your-api-key-here

# City for weather display (format: "City,CountryCode")
WEATHER_CITY=Detroit,US

# Temperature units (imperial or metric)
WEATHER_UNITS=imperial

# How often to check weather API (seconds, default: 1800 = 30min)
WEATHER_CHECK_INTERVAL=1800

# === Stocks Settings ===
# Stock symbols to track (comma-separated)
# Examples: AAPL, GOOGL, MSFT, TSLA, SPY, QQQ, AMZN
STOCKS_SYMBOLS=AAPL,GOOGL,MSFT,TSLA

# How often to check stock prices (seconds, default: 300 = 5min)
STOCKS_CHECK_INTERVAL=300

# === Sports Settings ===
# Teams to follow (comma-separated team abbreviations)
# Note: Teams are league-specific, so ATL in MLB won't match ATL in NBA
# Examples: DET, BOS, NYR, CHI, ATL, etc.
SPORTS_NHL_TEAMS=DET
SPORTS_NBA_TEAMS=DET
SPORTS_NFL_TEAMS=DET
SPORTS_MLB_TEAMS=DET

# Test mode: Pick 2 random live games (useful for testing layouts)
# Set to true to test layouts when your teams aren't playing
SPORTS_TEST_MODE=false

# How often to check for score updates (seconds, default: 10)
SPORTS_CHECK_INTERVAL=10

# === Display Settings ===
# Auto-switch to sports when live games detected (true/false)
DISPLAY_SPORTS_PRIORITY=true

# Modes to cycle through (comma-separated: sports, clock, weather)
DISPLAY_CYCLE_MODES=clock,weather

# How long to show each mode before cycling (seconds)
DISPLAY_CYCLE_SECONDS=300

# Clock theme: stranger_things, classic, or matrix
CLOCK_THEME=stranger_things

# Weather forecast mode: daily or hourly
WEATHER_FORECAST_MODE=daily

# 24-hour clock format (true/false)
CLOCK_24H=false

# === Performance Settings ===
# PNG refresh intervals (seconds, keeps displays visible)
DISPLAY_SPORTS_REFRESH_INTERVAL=2
DISPLAY_WEATHER_REFRESH_INTERVAL=2
DISPLAY_CLOCK_REFRESH_INTERVAL=2
DISPLAY_STOCKS_REFRESH_INTERVAL=2

# How often to check for mode changes (seconds, default: 2)
DISPLAY_MODE_CHECK_INTERVAL=2

# === Visual Preferences ===
# Show team logos in sports mode (true/false)
SPORTS_SHOW_LOGOS=true

# Show weather icons (true/false)
WEATHER_SHOW_ICONS=true

